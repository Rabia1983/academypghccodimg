<h1>Editing postits</h1>

<%= form_for :postit, url: postit_path(@postit), method: :patch do |f| %>

  <% if @postit.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@postit.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @postit.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to 'Back', postits_path %>
